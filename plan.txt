1. hr_record 
    - описываем сущности проекта
        - {id, //уникальный ИД 
        fullname, //фио 
        position, //должность 
        reception_at} //время трудоустройства

2. repository
            2.1 Интерфейс реализующий //interface <- package
                -Добавление работника 
                -Получение всех работников
                -Удаление работника 

    2.2 Структура состоящая из слайс/мап сущности + функция конструктор 

    2.3 Реализация метедов интерфейса и валидация на том же уровне 

3. http
    3.1 Структура из указателей на структуру репозитория + функция конструктор 

    3.2 Методы реализующие требуемый функционал приложения 

    3.3 dto для хранения сущностей ввода и вывода json http (сотрудники(ввод/вывод) и ошибки по формату json)

    3.4 Подключение к http серверу, возвращаемое в main

4. database 
    4.1 model слой структур получения и внесения донных в database
    
    4.2 подмена реализации слоя репозитория уже с сохранением не в переменные а в базу данных

5. Реализация миграций
6. Заполнение Makefile и .env (с созданием переменных окружения)

подготовка main.go файла

7. Заполнение DockerFile для создания образа 
8. Заполенние docker-compose файла с внесением запуска в Makefile и реализация проброса портов и docker volume
        Условие подготовка начального билда на этапе создания образа для избежания 
        повторного преобразования программы в бинарный код при каждом запуске 


